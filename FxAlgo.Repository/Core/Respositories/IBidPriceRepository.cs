namespace FxAlgo.Repository
{
    public interface IBidPriceRepository : IRepository<BidPrice>
    {
        //BidPrice GetPrice(int id);
        //IEnumerable<BidPrice> GetPrices(int pageIndex, int pageSize);
        //void AddPrice(BidPrice bidPrice);
        //void AddPrices(IEnumerable<BidPrice> bidPrices);

        //Custom queries
    }
}
